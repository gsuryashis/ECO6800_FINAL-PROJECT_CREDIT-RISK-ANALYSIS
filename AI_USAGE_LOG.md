# AI Usage Log

| Date | Tool | What you used it for | What you verified yourself |
|---|---|---|---|
| 2026-05-05 | Copilot | Restructured flat repo into professional `src/`, `data/`, `outputs/` layout; added `requirements.txt` and `.gitignore`; rewrote README with How-to-Run and Results sections (PR #2) | Confirmed all six pipeline scripts moved to `src/`, `.gitignore` excluded CSV files correctly, and README steps were accurate |
| 2026-05-05 | Copilot | Wired all four pipeline scripts (`woe2.py`, `model.py`, `score_card.py`, `EDA.py`) to write outputs under `outputs/tables/` and `outputs/figures/` using `os.path.dirname`; added `plt.savefig` calls for figure exports; committed `final_scorecard.csv` (PR #3) | Ran each script manually, checked that output CSVs and PNG figures appeared in the expected directories |
| 2026-05-05 | Copilot | Created `CHARTER.md`, `scripts/probe_data.py`, and `main.py` pipeline entry point (logistic regression baseline → LightGBM → JSON metric outputs); added `lightgbm` to `requirements.txt`; updated README with four-step quick-start (PR #4) | Ran `python main.py`, inspected `outputs/primary_metric.json` and `outputs/milestone_manifest.json`, verified `passed` field matched 0.72 ROC-AUC threshold |
| 2026-05-05 | Copilot | Added milestone/final output files, SHAP scorecard comparison, and honest model-performance report; added lift calculation and updated pass condition in `main.py` (PR #5) | Reviewed SHAP values and scorecard comparison table, confirmed lift calculation logic and that `passed` condition was consistent with stated threshold |
| 2026-05-08 | Copilot | Added committed fallback sample dataset (`data/fallback/lc_loan.csv`) so pipeline runs on a clean machine without the private raw CSV; updated `main.py` and `scripts/probe_data.py` to detect and use fallback; updated README quick-start (PR #6) | Deleted `data/raw/lc_loan.csv` locally, ran `python main.py`, confirmed pipeline completed using fallback data and manifest reported correct data mode |
| 2026-05-09 | Copilot | Repaired fresh-clone reproducibility: added `pyproject.toml` with runtime dependencies and `[build-system]` metadata; made `requirements.txt` delegate to `.`; hardened `primary_metric.json` JSON serialization; expanded `milestone_manifest.json` source-access proof; updated README for `uv run` path (PR #8) | Ran `uv run main.py` from a clean state, confirmed `outputs/primary_metric.json` parsed as valid JSON, checked manifest contained source and probe fields |
| 2026-05-09 | Copilot | Updated README installation steps with Windows-specific `uv` install commands (PowerShell) and clarified session-local PATH behavior (PR #10) | Read updated README steps on Windows and verified command syntax matched `uv` documentation |
| 2026-05-09 | Copilot | Fixed `outputs/milestone_manifest.json` to be a single valid JSON object (removed duplicate root-object concatenation); updated `main.py` to always emit `charter_locked`, `sources`, and `baseline_ready` fields deterministically (PR #11) | Ran `python -m json.tool outputs/milestone_manifest.json` to confirm valid parse; checked all required milestone fields were present |
| 2026-05-12 | Copilot | Drafted wording cleanup for source-access consistency across README/CHARTER/report | Re-read each file and checked instructions matched `main.py` fallback + probe behavior |
| 2026-05-12 | Copilot | Proposed expanded `milestone_manifest.json` source listing structure in `main.py` to include external URLs, raw/fallback path presence, probe script metadata, and `source_access_proof` block (PR #14) | Ran `python main.py` and inspected `outputs/milestone_manifest.json` for correct source, probe, and fallback entries |
| 2026-05-12 | Copilot | Suggested concise AI log format and concrete entries | Confirmed entries reflect actual tasks completed in this repo and removed vague wording |
| 2026-05-12 | Copilot | Added evidence tables/figures (WoE/IV, SHAP, grade rates), expanded manifest outputs, and strengthened README/report with submission checklist and economics interpretation | Ran `python main.py`, inspected new outputs and report evidence map for correctness |
